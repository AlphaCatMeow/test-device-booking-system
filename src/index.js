import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // 可以在这里引入全局样式
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
